<!-- Navbar.ejs -->
<nav id="navbar" class="bg-transparent m-0 p-0 backdrop-blur-lg">
    <div class="flex flex-wrap justify-between items-center mx-auto max-w-screen-xl py-2 px-6">
        <a href="https://flowbite.com" class="flex items-center space-x-3 rtl:space-x-reverse">
            <!-- Logo image link should be fixed if necessary -->
            <span class="self-center text-2xl font-semibold whitespace-nowrap text-black">The OrnateWall</span>
        </a>
        <button onclick="toggleMenu()" class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-gray-200">
            <span class="sr-only">Open main menu</span>
            <!-- SVG remains the same -->
        </button>
    </div>
    <!-- Dynamic rendering similar to React but controlled with CSS and simple JS -->
    <!-- Other HTML remains largely unchanged, you add JavaScript to handle menu toggling and dropdowns similar to the above -->
</nav>

<script>
    let isMenuOpen = false;

    function toggleMenu() {
        const menu = document.getElementById('mega-menu-full');
        isMenuOpen = !isMenuOpen;
        menu.style.display = isMenuOpen ? 'block' : 'none';
    }

    function toggleDropdown() {
        const dropdown = document.getElementById('mega-menu-full-dropdown');
        dropdown.classList.toggle('hidden');
    }
</script>
